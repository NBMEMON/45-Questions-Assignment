function make_shirts(shirtsize : String ="Large", printmessage : string ="I love typescript"){
    console.log(`\nMake a shirt of ${shirtsize} size and print ${printmessage} on it`)
    }
    // calling with default values
    make_shirts()

    // calling with custom values
    make_shirts("Small","DSA Next Goal")